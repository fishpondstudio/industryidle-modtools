{
    "name": "modtools",
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "serve": "vite preview"
    },
    "dependencies": {
        "date-fns": "^2.28.0",
        "preact": "^10.7.0"
    },
    "devDependencies": {
        "@preact/preset-vite": "^2.2.0",
        "typescript": "^4.6.3",
        "vite": "^2.9.1"
    }
}
